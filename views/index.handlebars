<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Portfolio</title>
</head>
<body>
  <h1>Bienvenidos a nuestro portfolio</h1>

  <a href="/trabajos">Trabajos realizados</a>
   <ul>
  {{#if trabajos.length}}
    {{#each trabajos}}
      <li>
        <h3>{{this.titulo}}</h3>
        <p>{{this.descripcion}}</p>
        <p>Empresa: {{this.Empresa}}</p>
        <p>Tecnologías: {{this.Tecnologias}}</p>
      </li>
    {{/each}}
  {{else}}
    <p>No se encontraron trabajos</p>
  {{/if}}
</ul>

  <h2>Miembros del equipo:</h2> 
   <ul>
  {{#if miembros.length}}
    {{#each miembros}}
      <li>
        <h3>{{this.nombre}} {{this.apellido}}</h3>
        <p>Titulación: {{this.titulacion}}</p>
        <p>Servicios: {{this.servicios}}</p>
        <p>Equipo: {{this.nombre_equipo}}</p>
        <a href="/miembro/{{this.id}}">Ver más detalles</a>
      </li>
    {{/each}}
  {{else}}
    <p>No se encontraron miembros</p>
  {{/if}}
</ul>

   <p><a href="/contacto">Contáctanos</a></p>
</body>
</html>




